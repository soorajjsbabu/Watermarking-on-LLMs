Install "Continue" or "CodeGPT Chat" extension for VS Code

To install Ollama:
ollama run llama3.1:8b   # smaller model
ollama run llama3.1:70b  # bigger model

Install libraries:
pip install requests numpy transformers sentence-transformers pandas 

Run Ollama
ollama serve

Run the python script